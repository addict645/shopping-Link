from admin_honeypot import views
from django.urls import re_path  # Make sure to import re_path here

app_name = 'admin_honeypot'

urlpatterns = [
    re_path(r'^login/$', views.AdminHoneypot.as_view(), name='login'),  # Replace url() with re_path()
    re_path(r'^.*$', views.AdminHoneypot.as_view(), name='index'),       # Replace url() with re_path()
]

